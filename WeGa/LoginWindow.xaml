<Window x:Class="WeGa.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login to Play" Height="200" Width="300" Background="#FFF5AEA3">
    <Grid Margin="10,0,10,10">
        <!--<Grid.Background>
            <ImageBrush ImageSource="anagram.png"  />
        </Grid.Background>-->

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <Border Grid.Column="0" Grid.ColumnSpan="2" Padding="5" Background="#4E87D4">
            <Label VerticalAlignment="Center" Foreground="White">Login</Label>
        </Border>

        <Label Grid.Column="0" Grid.Row="1">Username:</Label>
        <Label Grid.Column="0" Grid.Row="2">Pasword:</Label>
        <TextBox x:Name="username"  Grid.Column="1" Grid.Row="1"  Style="{StaticResource loginTextboxStyle}" Text=""/>
        <PasswordBox x:Name="password" Grid.Column="1" Grid.Row="2" Style="{StaticResource loginPasswordStyle}" />
        <Button x:Name="btnLogin" Grid.Column="1" Grid.Row="3" Style="{StaticResource buttonStyle}" Content="Login" Click="btnLogin_Click"/>
    </Grid>
</Window>
